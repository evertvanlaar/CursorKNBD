{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Live Server (poort 5501)",
            "type": "shell",
            "command": "npx --yes live-server --port=5501 --open=info.html",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "isBackground": true,
            "problemMatcher": [],
            "presentation": {
                "reveal": "always",
                "panel": "dedicated"
            }
        }
    ]
}
