# A3 — Release notes (website 3.1.138)

**Doel:** OneSignal-testerdocumentatie bijwerken (kaart + browserprompt + switch). Geen publieke feature.

**Website:** `asset-version.txt` → **3.1.138** (via `scripts/sync-asset-version.mjs`)

**Android / Play:** geen nieuwe TWA-build; geen store-listing-wijziging.

Zie [`docs/onesignal-web-push.md`](../onesignal-web-push.md).

---

## Wat is nieuw (3.1.136–3.1.138, testers)

1. OneSignal-bel uit. Beheer via More (mobiel) en footer (desktop), alleen `?onesignal=1`.
2. Eerste-opt-in kaart Allow / Not now; Allow start de echte browserprompt.
3. Switch volgt de browsertoestemming (intrekken via slotje → Off, geen valse On).

Bezoekers zonder tester-vlag zien niets.

---

## English (EN) — kort

Niet voor Play “What’s new”.

```
• Internal: web push tester UI (not shown to visitors)
```

## Greek (EL) — kort

Niet voor Play “What’s new”.

```
• Εσωτερικό: δοκιμή web push (δεν εμφανίζεται στους επισκέπτες)
```
